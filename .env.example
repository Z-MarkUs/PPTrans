# Copy to .env, add only the credential you intend to use, and select it with
# --env-file .env. PPTrans never discovers dotenv files implicitly or prints
# credential values. The identity provider needs no key.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
